import threading

#数据包待检测列表类：
#    同步锁,
#    数据:
#        0：方法类型；
#        1：源IP地址；
#        2：URL；
#        3：检测字段；
class DataPacket:
    def __init__(self):
        self.mu = threading.Condition()
        self.data = [[]]
    def dequeue(self):
        if self.mu.acquire():
            res = self.data[0]
            del self.data
            self.mu.releas()
        return res
    def enqueue(self, var):
        if self.mu.acquire():
            self.data.append(var)
            self.mu.releas
        return


